{% extends 'common/layout.html.twig' %}

{% block content %}
<div class="page-header">
  <h1>Edit Merchant <small>{{ merchant.name }}</small></h1>
</div>

<div class="row">
  <div class="col-md-9">
    {{ form_start(form, { attr: { class: 'form-horizontal', role: 'form' } }) }}
      <div class="form-group">
        <label class="col-md-2 control-label">Store Name</label>
        <div class="col-md-6">
          <p class="form-control-static">{{ merchant.name }}</p>
        </div>
      </div>
      <div class="form-group">
        <label class="col-md-2 control-label">Commission</label>
        <div class="col-md-4">
          {{ form_widget(form.commission, { attr: { class: 'form-control' } }) }}
        </div>
      </div>
      <div class="form-group">
        <label class="col-md-2 control-label">Comm. Type</label>
        <div class="col-md-3">
          {{ form_widget(form.commissionType, { attr: { class: 'form-control' } }) }}
        </div>
      </div>
      <div class="form-group">
        <div class="col-md-6 col-md-offset-2">
          <button type="submit" class="btn btn-primary">Update</button>
        </div>
      </div>
    {{ form_end(form) }}
  </div>
</div>
{% endblock %}
